<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="BulletinMapper">
    <!-- 단일행 조회를 위한 기능 정의 -->
    <select id="selectItem">
        SELECT b.title, b.postdate, b.bgcolor, b.bgimage, b.content, u.username
        FROM bulletin AS b
        LEFT JOIN user AS u
        ON b.user_id = u.id
        WHERE id=#{id};
    </select>

    <!-- 다중행 조회를 위한 기능 정의 -->
    <select id="selectList">
        SELECT b.id, u.username, b.title, b.postdate, b.bgcolor, b.bgimage, b.content
        FROM bulletin AS b
        LEFT JOIN user AS u
        ON u.id = b.user_id
        ORDER BY b.id DESC

        <!-- 페이지 구현을 위한 LIMIT절 추가 -->
        <if test="liscCount > 0">
            LIMIT ${offset}, ${listCount}
        </if>
    </select>

    <!-- 태그 조회를 위한 기능 정의 -->
    <select id="selectTagsOfPosts">
        SELECT bc.bulletin_id, c.name
        FROM bulletin_category AS bc
        LEFT JOIN category AS c
        ON bc.category_id = c.id
        ORDER BY bulletin_id ASC
    </select>

    <!-- 데이터 수 조회하기 -->
    <select id="selectAllCount">
        SELECT COUNT(*) AS cnt FROM bulletin
    </select>

    <!-- 내가 댓글쓴 장소 목록 조회 -->
    <select id="selectMyPlaces">
        SELECT place_id FROM review WHERE user_id = #{id}
    </select>
</mapper>